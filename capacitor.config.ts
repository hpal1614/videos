import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wizardschess.app',
  appName: "Wizard's Chess",
  webDir: 'dist',
  backgroundColor: '#07060c',
};

export default config;
